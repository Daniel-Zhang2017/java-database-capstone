spring.application.name=back-end

spring.web.resources.static-locations=classpath:/static/

#Defines the base directory for Thymeleaf templates. Spring will look for templates inside src/main/resources/templates/.
spring.thymeleaf.prefix=classpath:/templates/

#Specifies that all Thymeleaf templates will have an .html suffix.
spring.thymeleaf.suffix=.html

#Sets the rendering mode for templates. 
#HTML mode is most commonly used for rendering well-formed HTML5 pages.
spring.thymeleaf.mode=HTML

#Disables caching of templates.
#Useful during development to see changes immediately without restarting the app.
spring.thymeleaf.cache=false
spring.thymeleaf.encoding=UTF-8

# application.properties
jwt.secret=your-secret-key-here-at-least-256-bits-long-for-hs256-algorithm
jwt.expiration=604800000 # 7 days in milliseconds

# ========================
# MySQL Configuration
# ========================
spring.datasource.url=jdbc:mysql://172.21.161.207:3306/cms?useSSL=false&serverTimezone=UTC&characterEncoding=utf8
spring.datasource.username=root
spring.datasource.password=6OcogzQEjdI6MPxUQR85Z8mr
# 连接池配置（可选但推荐）
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# ========================
# JPA Configuration
# ========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# 明确指定方言（根据你的MySQL版本）
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
# 如果是MySQL 5.x，使用：
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect
spring.jpa.open-in-view=false
# 命名策略（可选）
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# ========================
# MongoDB Configuration
# ========================
# 方案1：使用属性方式（当前使用）
spring.data.mongodb.host=172.21.156.7
spring.data.mongodb.port=27017
spring.data.mongodb.database=prescriptions
spring.data.mongodb.username=root
spring.data.mongodb.password=29FdJtOkSF7RcTC8Lg5XhBRl
spring.data.mongodb.authentication-database=admin
# MongoDB连接池配置（可选）
spring.data.mongodb.auto-index-creation=true
spring.data.mongodb.connection-timeout=30000
spring.data.mongodb.socket-timeout=60000

# 或者方案2：使用URI方式（注释掉上面的属性方式）
# spring.data.mongodb.uri=mongodb://root:dc4nk3KQqJGFRRBLRIYnPKW0@172.21.19.148:27017/prescriptions?authSource=admin&retryWrites=true&w=majority

# ========================
# 应用通用配置
# ========================
# 服务器端口（如果使用默认8080可不配置）
server.port=8080
# 上下文路径
server.servlet.context-path=/

# 文件上传大小限制
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ========================
# Actuator Configuration
# ========================
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always
management.health.db.enabled=true
management.info.env.enabled=true

# ========================
# 业务相关配置
# ========================
api.path=/
jwt.secret=$!@#$^%$$$%####$DDCPN0234FCFDPD8670M
jwt.expiration=86400000  # 24小时，单位毫秒（可选）

# ========================
# 静态资源和模板
# ========================
spring.web.resources.static-locations=classpath:/static/
# 添加缓存控制（生产环境推荐）
# spring.web.resources.cache.period=3600

# Thymeleaf Configuration
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.cache=false
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.check-template-location=true

# ========================
# 日志配置（可选但推荐）
# ========================
logging.level.com.project.back_end=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE
logging.level.org.springframework.data.mongodb=DEBUG
logging.file.name=logs/application.log
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# ========================
# 开发工具配置
# ========================
# 如果你使用spring-boot-devtools
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true